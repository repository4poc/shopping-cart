version: '2'
services:
  shoppingcartservice:
    image: docker.io/varindergupta/shoppingcartservice
    ports:
      - "8081:8080"
    expose:
      - "8081"
    command: --timeout=500 -- catalina.sh run


version: '2'
services:
  shoppingcartservice:
    image: docker.io/varindergupta/shoppingcartservice
    ports:
      - "8080:8080"
    expose:
      - "8080"
